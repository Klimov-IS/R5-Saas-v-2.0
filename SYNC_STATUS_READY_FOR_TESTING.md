# ✅ Синхронизация НЕ пропущена - Готово к тестированию

**Дата:** 2026-01-16
**Статус:** Все компоненты синхронизации реализованы и готовы к запуску

---

## 📋 Что было сделано

### 1. ✅ Обновлён эндпоинт синхронизации диалогов
**Файл:** `src/app/api/stores/[storeId]/dialogues/update/route.ts`

**Ключевые изменения:**
- ✅ Интегрирована новая AI классификация `classifyChatDeletion` (строки 181-198)
- ✅ Добавлено сохранение всех 12 тегов, включая deletion workflow (строки 216-230)
- ✅ Rate limiting: 1.5 секунды между запросами к WB API (строка 107)
- ✅ Пересчёт статистики по новым тегам: deletion_candidate, deletion_offered и т.д.

**Защита от лимитов WB API:**
```typescript
// Строка 107
await new Promise(res => setTimeout(res, 1500)); // API rate limiting
```

### 2. ✅ Создан эндпоинт массовой синхронизации
**Файл:** `src/app/api/stores/dialogues/update-all/route.ts`

**Функционал:**
- ✅ Синхронизирует все магазины последовательно
- ✅ Дополнительная задержка 2 секунды между магазинами (строка 129)
- ✅ Детальный лог результатов по каждому магазину
- ✅ Возвращает итоговую статистику: успешно/ошибки

**Двойная защита от лимитов:**
1. 1.5 сек между событиями внутри магазина
2. 2 сек между магазинами

### 3. ✅ UI компоненты для deletion workflow

**Обновлённые компоненты:**
- `src/components/chats/ConversationPanel.tsx` - отображение всех 12 тегов (строки 17-31)
- `src/components/chats/DeletionCaseInfo.tsx` - новый компонент для deletion cases
- `src/components/chats/FilterPanel.tsx` - фильтры по новым тегам

**Новый API эндпоинт:**
- `src/app/api/stores/[storeId]/deletion-cases/[chatId]/route.ts` - получение deletion case

---

## 🚀 Как запустить синхронизацию

### Вариант 1: Синхронизация всех магазинов (рекомендуется для первого запуска)

```bash
curl -X POST "http://localhost:9002/api/stores/dialogues/update-all" \
  -H "Authorization: Bearer wbrm_u1512gxsgp1nt1n31fmsj1d31o51jue" \
  -H "Content-Type: application/json" \
  -w "\n\nHTTP Status: %{http_code}\nВремя: %{time_total}s\n" \
  -s
```

**Ожидаемый результат:**
```json
{
  "message": "Синхронизация завершена: 2 магазинов обработано, 2 успешно, 0 ошибок.",
  "results": [
    {
      "storeId": "0rCKlFCdrT7L3B2ios45",
      "storeName": "ИП Соколов А. А.",
      "status": "success",
      "message": "Successfully updated dialogues..."
    },
    {
      "storeId": "TwKRrPji2KhTS8TmYJlD",
      "storeName": "Rating5 OPS",
      "status": "success",
      "message": "Successfully updated dialogues..."
    }
  ]
}
```

### Вариант 2: Синхронизация одного магазина

```bash
curl -X POST "http://localhost:9002/api/stores/TwKRrPji2KhTS8TmYJlD/dialogues/update" \
  -H "Authorization: Bearer wbrm_u1512gxsgp1nt1n31fmsj1d31o51jue" \
  -H "Content-Type: application/json" \
  -w "\n\nHTTP Status: %{http_code}\nВремя: %{time_total}s\n" \
  -s
```

---

## 🔍 Мониторинг логов (защита от лимитов)

### Что смотреть в консоли

**Нормальная работа:**
```
[DIALOGUES] Starting dialogue update for store: TwKRrPji2KhTS8TmYJlD
[DIALOGUES] Fetching active chats for store TwKRrPji2KhTS8TmYJlD...
[DIALOGUES] Found 47 active chats.
[DIALOGUES] Updated/Created 47 chat documents.
[DIALOGUES] Fetching new message events starting from cursor: null
[DIALOGUES] Fetched 128 new message events.
[DIALOGUES] Starting AI tag classification for 15 updated chats...
[DIALOGUES] Chat abc123: Classified as 'deletion_candidate'.
[DIALOGUES] Chat xyz456: Classified as 'active'.
[DIALOGUES] AI classification complete: 15 successful, 0 errors.
[DIALOGUES] Final stats: totalChats=47, tagCounts= { deletion_candidate: 3, active: 12, ... }
```

**Признаки проблем с лимитами WB API:**
```
Error fetching WB events: 429 Too Many Requests
Error fetching WB chats: 503 Service Unavailable
```

**Если увидите ошибку 429/503:**
1. Увеличить задержку в строке 107: `setTimeout(res, 3000)` (вместо 1500)
2. Увеличить задержку в update-all строка 129: `setTimeout(res, 5000)` (вместо 2000)

---

## 📊 Проверка результатов после синхронизации

### 1. Проверить количество чатов

```bash
curl -X GET "http://localhost:9002/api/stores/TwKRrPji2KhTS8TmYJlD" \
  -H "Authorization: Bearer wbrm_u1512gxsgp1nt1n31fmsj1d31o51jue" \
  -s | grep -E '"totalChats"|"chatTagCounts"'
```

**Ожидаемый результат:**
```json
{
  "totalChats": 47,
  "chatTagCounts": {
    "active": 12,
    "deletion_candidate": 3,
    "no_reply": 8,
    "successful": 10,
    "untagged": 14
  }
}
```

### 2. Проверить чаты с тегом deletion_candidate

```bash
curl -X GET "http://localhost:9002/api/stores/TwKRrPji2KhTS8TmYJlD/chats?limit=50" \
  -H "Authorization: Bearer wbrm_u1512gxsgp1nt1n31fmsj1d31o51jue" \
  -s | grep -B2 -A5 '"tag":"deletion_candidate"'
```

**Что искать:**
- Должны быть чаты с тегом `"tag":"deletion_candidate"`
- Эти чаты содержат ключевые фразы: "удалю отзыв", "верните деньги удалю", etc.

### 3. Открыть UI и проверить фильтры

1. Запустить dev сервер: `npm run dev`
2. Открыть: http://localhost:9002/stores/TwKRrPji2KhTS8TmYJlD/chats
3. Проверить левую панель фильтров - должен быть раздел "🎯 Удаление отзывов"
4. Должны быть видны счётчики: "🎯 Кандидаты (3)", "💰 Предложена компенсация (0)" и т.д.

---

## 🧪 Тестирование генерации предложений

### После успешной синхронизации

1. **Найти deletion_candidate чаты в UI:**
   - Открыть http://localhost:9002/stores/TwKRrPji2KhTS8TmYJlD/chats
   - Кликнуть фильтр "🎯 Кандидаты"
   - Выбрать чат

2. **Сгенерировать предложение:**
   - Справа увидите карточку "🎯 Кандидат на удаление отзыва"
   - Кнопка "Сгенерировать предложение компенсации"
   - Кликнуть и дождаться ответа (3-5 секунд)

3. **Оценить качество:**
   - Прочитать сгенерированное сообщение
   - Проверить сумму компенсации (должна соответствовать правилам артикула)
   - Оценить тон: вежливый, не прямолинейный, без упоминания "удалить отзыв"

4. **Повторить на 5-10 чатах:**
   - Собрать статистику качества ответов
   - Записать примеры хороших и плохих генераций
   - Через несколько дней итерировать промпт

---

## ⚠️ Известные риски и как их избежать

### Риск 1: Лимиты WB API
**Симптомы:** Ошибки 429 Too Many Requests
**Решение:** Увеличить задержки в route.ts (см. выше)

### Риск 2: AI классификация падает
**Симптомы:** "Chat xyz: Classification failed"
**Решение:** Проверить API ключ Deepseek, проверить баланс

### Риск 3: Нет deletion_candidate чатов
**Возможные причины:**
- В реальных чатах действительно нет подходящих фраз
- Regex не сработал (проверить chat_deletion_helpers.ts)
- AI не нашёл паттерны (проверить промпт)

**Решение:**
- Посмотреть логи: искать "Classified as"
- Проверить вручную текст чатов на фразы типа "удалю отзыв"

---

## 📝 Чек-лист перед запуском

- [ ] Dev сервер запущен: `npm run dev`
- [ ] PostgreSQL доступна (проверить .env.local)
- [ ] Deepseek API ключ настроен в settings
- [ ] WB API токены настроены для магазинов
- [ ] Миграции применены (все 3 файла от 2026-01-16)
- [ ] Открыта консоль для мониторинга логов

---

## 🎯 Следующие шаги после синхронизации

1. ✅ Запустить синхронизацию (сейчас)
2. ✅ Проверить логи на ошибки лимитов (через 5 минут)
3. ✅ Открыть UI и найти deletion_candidate чаты
4. ✅ Протестировать генерацию на 5-10 чатах
5. 📊 Собрать статистику качества ответов (несколько дней)
6. 🔄 Итерировать промпт при необходимости
7. 🚀 Запустить автоматическую отправку (когда будет готов)

---

## 📞 Что делать если что-то пошло не так

### Ошибка: "Store not found"
- Проверить ID магазина в команде
- Выполнить: `SELECT id, name FROM stores;`

### Ошибка: "Chat API token not configured"
- Проверить: `SELECT id, chat_api_token FROM stores WHERE id = '...';`
- Добавить токен через UI или SQL

### Ошибка: "Failed to fetch deletion case"
- Это нормально если deletion case ещё не создан
- Появится после клика "Сгенерировать предложение"

### AI возвращает некорректный JSON
- Проверить логи Deepseek API
- Проверить баланс API ключа
- Попробовать вручную через `/api/stores/{storeId}/chats/classify-deletion`

---

## ✅ Итого: Синхронизация реализована

**Синхронизация НЕ пропущена!** Все компоненты готовы:

1. ✅ Обновлён `/dialogues/update` с новой AI классификацией
2. ✅ Создан `/dialogues/update-all` для массовой синхронизации
3. ✅ Rate limiting: 1.5 сек между событиями + 2 сек между магазинами
4. ✅ UI компоненты для deletion workflow
5. ✅ API эндпоинт для получения deletion cases
6. ✅ Comprehensive testing guide

**Можно запускать прямо сейчас!** 🚀

Команда для старта:
```bash
curl -X POST "http://localhost:9002/api/stores/dialogues/update-all" \
  -H "Authorization: Bearer wbrm_u1512gxsgp1nt1n31fmsj1d31o51jue" \
  -H "Content-Type: application/json" \
  -w "\n\nHTTP Status: %{http_code}\nВремя: %{time_total}s\n" -s
```

Через 5 минут проверить консоль на ошибки лимитов WB API.
