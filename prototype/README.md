# 🎨 HTML Прототип - WB Reputation Manager

Быстрый HTML/CSS прототип для разработки дизайна без компиляции Next.js.

## 📂 Структура файлов

```
prototype/
├── index.html              # Главная страница (все магазины)
├── styles.css              # R5 Design System (общие стили)
└── README.md               # Эта инструкция
```

## 🚀 Как использовать

### Способ 1: Открыть напрямую в браузере

1. Найдите файл `prototype/index.html` в проводнике
2. Кликните правой кнопкой → **Открыть с помощью** → выберите браузер (Chrome/Edge/Firefox)
3. Готово! Страница откроется мгновенно

### Способ 2: Через VSCode (Live Server)

1. Установите расширение **Live Server** в VSCode
2. Откройте `prototype/index.html`
3. Правой кнопкой → **Open with Live Server**
4. Изменения будут отображаться автоматически при сохранении (Ctrl+S)

### Способ 3: Через терминал (Python)

```bash
cd "wb-reputation V 1.0/prototype"
python -m http.server 8000
```

Откройте http://localhost:8000/index.html

## ✏️ Как редактировать

### 1. Изменить цвета категорий

Откройте `styles.css`, найдите секцию **CSS Variables**:

```css
:root {
  /* Category Colors - Reviews */
  --category-reviews-bg: #f3f4f6;     /* Фон для отзывов */
  --category-reviews-text: #374151;   /* Текст для отзывов */

  /* Category Colors - Chats */
  --category-chats-bg: #dbeafe;       /* Фон для чатов */
  --category-chats-text: #1e40af;     /* Текст для чатов */

  /* ... и так далее */
}
```

Измените HEX-коды и сохраните (Ctrl+S), затем обновите браузер (F5).

### 2. Изменить раскладку KPI

Откройте `index.html`, найдите секцию **Сводная статистика**:

```html
<div class="kpi-container">
  <!-- 4 KPI карточки в горизонтальную линию -->
</div>
```

**Горизонтальная раскладка** (текущая):
```css
/* styles.css */
.kpi-container {
  display: flex;              /* Горизонтально */
  flex-direction: row;
}
```

**Вертикальная раскладка** (старая):
```css
/* styles.css */
.kpi-container {
  display: grid;              /* Сетка */
  grid-template-columns: repeat(4, 1fr);
}
```

### 3. Изменить данные в таблице

Откройте `index.html`, найдите секцию **Список магазинов**:

```html
<tbody>
  <tr>
    <td>
      <div>Магазин электроники "ТехноМир"</div>
      <div>Добавлен: 15 дек 2024</div>
    </td>
    <td><span class="badge">342</span></td>
    <!-- ... -->
  </tr>
</tbody>
```

Измените текст, цифры, даты напрямую в HTML.

### 4. Добавить новую карточку действия

Откройте `index.html`, найдите секцию **Быстрые действия**:

```html
<div class="actions-grid">
  <!-- Скопируйте блок .action-card -->
  <div class="action-card">
    <div class="action-icon" style="background-color: #dbeafe;">
      <i data-lucide="icon-name"></i>
    </div>
    <h3 class="action-title">Заголовок</h3>
    <p class="action-description">Описание</p>
    <button class="btn btn-primary btn-full">Кнопка</button>
  </div>
</div>
```

Список иконок Lucide: https://lucide.dev/icons/

## 🎨 R5 Design System

### Цветовая палитра

| Категория | Фон (bg) | Текст (text) | Использование |
|-----------|----------|--------------|---------------|
| **Reviews** | `#f3f4f6` | `#374151` | Отзывы, рейтинги |
| **Chats** | `#dbeafe` | `#1e40af` | Диалоги, сообщения |
| **Products** | `#f3e8ff` | `#6b21a8` | Товары, каталог |
| **Warnings** | `#fef3c7` | `#92400e` | Предупреждения, ошибки |

### Spacing Scale

```css
--spacing-xs: 4px;
--spacing-sm: 8px;
--spacing-md: 12px;
--spacing-lg: 16px;
--spacing-xl: 20px;
--spacing-2xl: 24px;
--spacing-3xl: 32px;
--spacing-4xl: 40px;
```

### Typography Scale

```css
--font-size-xs: 12px;   /* Маленький текст */
--font-size-sm: 14px;   /* Обычный текст */
--font-size-base: 16px; /* Базовый размер */
--font-size-lg: 18px;   /* Заголовки H3 */
--font-size-xl: 20px;   /* Заголовки H2 */
--font-size-2xl: 24px;  /* Заголовки H1, KPI цифры */
```

### Компоненты

#### Badge (Бэйдж)

```html
<span class="badge badge-blue">
  <i data-lucide="icon"></i>
  Текст
</span>
```

Варианты: `badge-blue`, `badge-purple`, `badge-gray`, `badge-yellow`

#### Button (Кнопка)

```html
<!-- Основная кнопка -->
<button class="btn btn-primary">Кнопка</button>

<!-- Кнопка с обводкой -->
<button class="btn btn-outline">Кнопка</button>

<!-- Кнопка на всю ширину -->
<button class="btn btn-primary btn-full">Кнопка</button>
```

#### Card (Карточка)

```html
<div class="card">
  <div class="card-content">
    <!-- Контент -->
  </div>
</div>
```

## 🔄 Workflow: Прототип → React

### Этап 1: Разработка дизайна в HTML

1. Открываете `index.html` в браузере
2. Редактируете HTML/CSS
3. Сохраняете (Ctrl+S)
4. Обновляете браузер (F5)
5. Повторяете, пока дизайн не станет идеальным

### Этап 2: Согласование

Когда дизайн готов:
- Показываете прототип (просто отправляете ссылку на файл)
- Согласовываете с командой/клиентом
- Вносите правки напрямую в HTML

### Этап 3: Перенос в React

После согласования дизайна:
1. Я перенесу точную копию HTML в React компоненты
2. Добавлю TypeScript типы
3. Подключу реальные API
4. Сохраню прототип как документацию

## 📱 Адаптивность

Прототип адаптивен:

- **Desktop (>1024px):** Горизонтальная раскладка KPI
- **Tablet (768-1024px):** Вертикальная раскладка KPI
- **Mobile (<768px):** Упрощенная раскладка

Измените размер окна браузера, чтобы увидеть адаптивность.

## 🐛 Отладка

### Проблема: Иконки не отображаются

**Решение:** Проверьте подключение Lucide:

```html
<!-- В <head> должно быть: -->
<script src="https://unpkg.com/lucide@latest"></script>

<!-- Перед </body> должно быть: -->
<script>
  lucide.createIcons();
</script>
```

### Проблема: Стили не применяются

**Решение:** Проверьте путь к CSS:

```html
<!-- В <head> должно быть: -->
<link rel="stylesheet" href="styles.css">
```

Убедитесь, что `styles.css` находится в той же папке, что и `index.html`.

### Проблема: Кириллица отображается крякозябрами

**Решение:** Проверьте кодировку:

```html
<!-- В <head> должно быть: -->
<meta charset="UTF-8">
```

Сохраните файл в кодировке UTF-8 в VSCode.

## ✅ Преимущества прототипа

1. **Мгновенные изменения** - нет компиляции Next.js
2. **Простота** - обычный HTML/CSS, без TypeScript
3. **Контроль** - весь код виден целиком
4. **Быстрая итерация** - изменения за секунды
5. **Легко показать** - просто откройте файл в браузере

## 📚 Дополнительные ресурсы

- **Lucide Icons:** https://lucide.dev/icons/
- **CSS Variables:** https://developer.mozilla.org/en-US/docs/Web/CSS/Using_CSS_custom_properties
- **Flexbox Guide:** https://css-tricks.com/snippets/css/a-guide-to-flexbox/
- **Grid Guide:** https://css-tricks.com/snippets/css/complete-guide-grid/

## 🎯 Следующие шаги

После согласования дизайна главной страницы, создам:

1. `store-dashboard.html` - дашборд одного магазина
2. `chats.html` - страница со списком чатов
3. `reviews.html` - страница с отзывами

---

**Вопросы?** Просто спросите! 🚀
