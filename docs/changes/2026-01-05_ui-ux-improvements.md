# UI/UX Design Improvements — План дизайнера

## 1. Анализ: Старый vs. Новый проект

### ❌ Текущие проблемы нового проекта:
1. **Отсутствие визуальной глубины** — карточки плоские, нет теней
2. **Слабая цветовая палитра** — только черный текст на белом фоне
3. **Нет визуальной иерархии** — все элементы одинаково важны
4. **Плохая типографика** — все тексты одного размера и веса
5. **Отсутствие цветных индикаторов** — badges не выделяются визуально
6. **Нет hover эффектов** — пользователь не понимает, что интерактивно

### ✅ Что хорошо работало в старом проекте:
1. **Четкая визуальная иерархия** — карточки с тенями и границами
2. **Цветные badges** — синие, зеленые, желтые, красные индикаторы статусов
3. **Контрастные кнопки** — синие primary кнопки выделяются
4. **Типографика** — четкое разделение заголовков (font-weight: 600) и body текста
5. **Hover состояния** — строки таблицы меняют цвет при наведении
6. **Иконки с цветом** — иконки в header имеют цветной фон (bg-primary/10)

---

## 2. План изменений (Sprint 7.4)

### 2.1. Улучшение Card компонентов
**Цель:** Добавить визуальную глубину и профессиональный вид

**Изменения:**
- ✅ Уже добавлено: `shadow-sm border`
- 🔄 Нужно усилить: `shadow-md` вместо `shadow-sm` для большей глубины
- ➕ Добавить: `rounded-lg` для скругленных углов (более современный вид)
- ➕ Добавить: `bg-card` для явного белого фона карточек

**Файлы:**
- `src/app/page.tsx` — 2 карточки
- `src/app/stores/[storeId]/page.tsx` — 5 карточек (4 KPI + 1 таблица)

---

### 2.2. Цветные Badges для статусов диалогов
**Цель:** Сделать статусы визуально различимыми

**Текущее состояние:**
```tsx
const tagConfig = {
  untagged: { label: "Без тега", color: "bg-gray-200", textColor: "text-gray-800" },
  active: { label: "Активный", color: "bg-blue-100", textColor: "text-blue-800" },
  // ...
}
```

**Проблема:** Цвета слишком бледные, не выделяются на белом фоне

**Решение:**
```tsx
const tagConfig = {
  untagged: {
    label: "Без тега",
    color: "bg-slate-200 dark:bg-slate-700",
    textColor: "text-slate-700 dark:text-slate-300",
    border: "border border-slate-300 dark:border-slate-600"
  },
  active: {
    label: "Активный",
    color: "bg-blue-500",
    textColor: "text-white",
    border: ""
  },
  successful: {
    label: "Успешный",
    color: "bg-green-500",
    textColor: "text-white",
    border: ""
  },
  unsuccessful: {
    label: "Неуспешный",
    color: "bg-red-500",
    textColor: "text-white",
    border: ""
  },
  no_reply: {
    label: "Без ответа",
    color: "bg-yellow-500",
    textColor: "text-white",
    border: ""
  },
}
```

**Файлы:**
- `src/components/stores/store-list.tsx:55-61`

---

### 2.3. Типографика и визуальная иерархия
**Цель:** Четкое разделение заголовков, подзаголовков и body текста

**Изменения:**

#### Page Headers:
```tsx
// Before: text-3xl font-semibold
// After: text-3xl font-bold

<h1 className="text-3xl font-bold tracking-tight">
  Все магазины
</h1>
```

#### Card Titles:
```tsx
// Усилить вес шрифта
<CardTitle className="text-sm font-semibold text-muted-foreground">
  Всего товаров
</CardTitle>
```

#### KPI Numbers:
```tsx
// Увеличить размер и вес
<div className="text-3xl font-bold">{products.length}</div>
```

**Файлы:**
- `src/app/page.tsx:88` — page header
- `src/app/stores/[storeId]/page.tsx:89,104,119,134` — KPI card titles
- `src/app/stores/[storeId]/page.tsx:95,110,125,140` — KPI numbers

---

### 2.4. Кнопки — визуальная иерархия
**Цель:** Первичные действия должны выделяться

**Текущее состояние:**
- ✅ "Добавить магазин" — `variant="default"` (синий)
- ✅ "Пересчитать статистику" — `variant="outline"` (вторичная)

**Дополнительно усилить:**
```tsx
// Добавить размер и вес для primary кнопок
<Button variant="default" size="default" className="font-semibold">
  <PlusCircle className="mr-2 h-4 w-4" />
  Добавить магазин
</Button>
```

**Файлы:**
- `src/app/page.tsx:112-115` — "Добавить магазин"
- `src/components/stores/store-list.tsx:193-195` — "Перейти"

---

### 2.5. Table — улучшенные hover эффекты
**Цель:** Четкая визуальная обратная связь при наведении

**Текущее состояние:**
- ✅ Уже добавлено: `hover:bg-muted/50 transition-colors`

**Усилить:**
```tsx
<TableRow
  key={store.id}
  className="hover:bg-muted/80 transition-all duration-200 cursor-pointer"
>
```

**Файлы:**
- `src/components/stores/store-list.tsx:167`

---

### 2.6. KPI Cards — цветные иконки
**Цель:** Визуально различать типы метрик

**Текущее состояние:**
```tsx
<Package className="h-4 w-4 text-muted-foreground" />
```

**Решение:**
```tsx
// Всего товаров — синий
<div className="bg-blue-100 dark:bg-blue-900 p-2 rounded-lg">
  <Package className="h-4 w-4 text-blue-600 dark:text-blue-300" />
</div>

// Всего отзывов — желтый (звезды)
<div className="bg-yellow-100 dark:bg-yellow-900 p-2 rounded-lg">
  <Star className="h-4 w-4 text-yellow-600 dark:text-yellow-300" />
</div>

// Всего диалогов — зеленый
<div className="bg-green-100 dark:bg-green-900 p-2 rounded-lg">
  <MessageSquare className="h-4 w-4 text-green-600 dark:text-green-300" />
</div>

// Активные диалоги — красный (alert)
<div className="bg-red-100 dark:bg-red-900 p-2 rounded-lg">
  <MessageCircle className="h-4 w-4 text-red-600 dark:text-red-300" />
</div>
```

**Файлы:**
- `src/app/stores/[storeId]/page.tsx:92,107,122,137` — иконки в KPI cards

---

### 2.7. Page Header — цветная иконка (как в старом проекте)
**Цель:** Добавить визуальный акцент в header

**Текущее состояние:**
```tsx
<div className="bg-primary/10 text-primary p-3 rounded-lg">
  <StoreIcon className="h-6 w-6" />
</div>
```

**Это уже правильно!** Нужно проверить, применяется ли стиль.

**Файлы:**
- `src/app/page.tsx:84-86`

---

## 3. Приоритизация изменений

### 🔴 Критично (сразу видно):
1. **Усилить тени на Cards** — `shadow-sm` → `shadow-md`
2. **Цветные badges** — `bg-blue-500 text-white` вместо `bg-blue-100`
3. **Цветные иконки в KPI Cards** — добавить обертки с `bg-blue-100 p-2 rounded-lg`

### 🟡 Важно (улучшает UX):
4. **Типографика** — увеличить font-weight для заголовков
5. **Hover эффекты** — усилить до `hover:bg-muted/80`

### 🟢 Опционально (полировка):
6. **Скругленные углы** — добавить `rounded-lg` к Cards
7. **Transitions** — добавить `transition-all duration-200`

---

## 4. Риски

1. **Цветовые сочетания** — нужно проверить контрастность для accessibility (WCAG 2.1)
2. **Dark mode** — все изменения должны работать в темной теме
3. **Browser cache** — пользователь должен сделать Ctrl+Shift+R для обновления стилей

---

## 5. Definition of Done

- [ ] Cards имеют явные тени (`shadow-md`) и скругленные углы
- [ ] Badges используют насыщенные цвета (bg-blue-500, bg-green-500, etc.)
- [ ] KPI Cards имеют цветные иконки с фоновыми обертками
- [ ] Заголовки используют font-bold вместо font-semibold
- [ ] Hover эффекты заметны при наведении на строки таблицы
- [ ] Проверено в светлой и темной теме
- [ ] Пользователь подтвердил, что дизайн выглядит профессионально

---

## 6. Порядок внедрения

### Sprint 7.4.1: Критичные визуальные улучшения (30 мин)
1. Усилить тени на Cards (`shadow-md`)
2. Изменить цвета badges на насыщенные
3. Добавить цветные обертки для иконок в KPI Cards

### Sprint 7.4.2: Типографика и hover эффекты (15 мин)
4. Увеличить font-weight для заголовков
5. Усилить hover эффекты в таблицах

### Sprint 7.4.3: Полировка (10 мин)
6. Добавить скругленные углы к Cards
7. Добавить плавные transitions

---

**Итого:** ~55 минут работы для достижения уровня дизайна старого проекта.
