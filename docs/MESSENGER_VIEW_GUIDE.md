# Messenger View - Руководство

## Обзор

Messenger View — это новый режим работы с чатами клиентов WB. Он предоставляет интерфейс мессенджера (WhatsApp/Telegram-подобный) для более эффективной работы с переписками.

## Ключевые возможности

### ✅ Реализовано в MVP

1. **Переключение режимов**
   - Таблица (Table View) - классический табличный вид
   - Мессенджер (Messenger View) - новый режим мессенджера

2. **Левая панель - Список чатов**
   - Поиск по клиенту, товару
   - Фильтры по тегам (Активные, Нет ответа, Успешные, Неуспешные)
   - Сворачиваемая панель фильтров и действий
   - Bulk selection (массовый выбор чатов)
   - Индикаторы непрочитанных сообщений (красная точка)
   - Сворачиваемый сайдбар (для экономии места)

3. **Правая панель - Переписка**
   - История сообщений клиента и продавца
   - Автоскролл к последнему сообщению
   - Выбор тега для чата

4. **AI Генерация ответов**
   - Генерация ответа с помощью Deepseek AI
   - Текст появляется прямо в поле ввода
   - Возможность редактирования сгенерированного текста
   - Повторная генерация (клик по кнопке снова)
   - Индикатор AI режима

5. **Отправка сообщений**
   - Отправка ответа в WB
   - Keyboard shortcuts:
     - `Ctrl+G` - генерация AI ответа
     - `Enter` - отправка сообщения

6. **Массовые действия**
   - Выбор нескольких чатов через чекбоксы
   - "Выбрать все" с indeterminate состоянием
   - Действия для выбранных чатов:
     - 🤖 Сгенерировать AI ответы
     - ✈️ Отправить ответы
     - 🏷️ Классифицировать AI
     - 🔄 Обновить
   - Если ничего не выбрано - действия применяются ко всем чатам

## Архитектура

### Компоненты

```
src/components/chats/
├── MessengerView.tsx          # Главный контейнер
├── ChatListSidebar.tsx        # Левая панель со списком
├── ChatItem.tsx               # Элемент списка чата
├── ConversationPanel.tsx      # Правая панель с перепиской
├── MessageBubble.tsx          # Отдельное сообщение
├── MessageComposer.tsx        # Поле ввода + AI
├── FilterPanel.tsx            # Панель фильтров и действий
└── BulkActionsBar.tsx         # Панель массового выбора
```

### State Management

- **Zustand Store** (`src/store/chatsStore.ts`) - UI состояние:
  - Режим отображения (table/messenger)
  - Активный чат
  - Выбранные чаты для bulk actions
  - Состояние сайдбара (свернут/развернут)
  - Фильтры и поиск

- **React Query** (`src/hooks/useChats.ts`) - Данные:
  - Список чатов с пагинацией
  - История сообщений
  - Mutations для AI генерации
  - Mutations для отправки сообщений
  - Bulk operations

### API Endpoints

```
GET    /api/stores/[storeId]/chats                      # Список чатов
GET    /api/stores/[storeId]/chats/[chatId]             # Один чат с историей
POST   /api/stores/[storeId]/chats/[chatId]/generate-ai # AI генерация
POST   /api/stores/[storeId]/chats/[chatId]/send        # Отправка сообщения
POST   /api/stores/[storeId]/chats/bulk/generate-ai     # Массовая AI генерация
POST   /api/stores/[storeId]/chats/bulk/send            # Массовая отправка
POST   /api/stores/[storeId]/chats/classify-all         # Массовая классификация
POST   /api/stores/[storeId]/dialogues/update           # Обновление с WB
```

## Использование

### Переключение режимов

На странице `/stores/[storeId]/chats` вверху справа находятся кнопки переключения:
- 📋 Таблица - классический вид
- 💬 Мессенджер - новый режим

### Работа с чатами

1. **Выбор чата**: Клик на чат в левой панели
2. **Просмотр истории**: История отображается в правой панели
3. **AI генерация**: Клик "Генерировать AI" или `Ctrl+G`
4. **Редактирование**: Текст можно редактировать в поле ввода
5. **Отправка**: Клик "Отправить" или `Enter`

### Массовые операции

1. **Выбор чатов**: Кликнуть чекбоксы на нужных чатах
2. **Выбрать все**: Использовать чекбокс "Выбрать все"
3. **Действия**: Раскрыть панель "Действия" в левой панели
4. **Выполнить**: Выбрать нужное действие

**Важно**: Если ничего не выбрано, действия применятся ко ВСЕМ чатам!

### Фильтры

#### Новая кнопка "Фильтры" в тулбаре

Вместо старой кнопки "Последний ответ" теперь есть универсальная кнопка **"Фильтры"** с двумя секциями:

**Секция 1: Последний написал** (взаимоисключающие):
- 💬 **От клиента** - показать только чаты, где последнее сообщение от клиента
- 📤 **От нас** - показать только чаты, где последнее сообщение от продавца
- ⚠️ Можно выбрать только один вариант или ни одного

**Секция 2: С черновиком**:
- 📝 **С черновиком** - показать только чаты с сохраненными черновиками ответов

#### Фильтры по статусам (Kanban)

В тулбаре есть кнопки статусов:
- 📥 Входящие (inbox)
- 🔄 В работе (in_progress)
- ⏳ Ожидание (awaiting_reply)
- ✅ Решено (resolved)
- 🔒 Закрыто (closed)

#### Фильтры по legacy тегам (устаревшие)

Раскрыть панель "Фильтры по тегам" в левой панели:
- 🟢 Активные
- 🟡 Нет ответа
- 🔵 Успешные
- 🔴 Неуспешные

**Важно:** Legacy теги скоро будут удалены, используйте новую систему статусов!

## Горячие клавиши

- `Ctrl+G` - Генерация AI ответа
- `Enter` - Отправка сообщения (в поле ввода)
- `Shift+Enter` - Перенос строки в поле ввода

## Toast Notifications

Система использует `react-hot-toast` для уведомлений:
- ✅ Успешная генерация AI
- ✅ Сообщение отправлено
- ❌ Ошибки при выполнении действий
- ℹ️ Информация о массовых операциях

## Что дальше (Roadmap)

### Must Have (P0)
- [ ] Real-time обновления через WebSocket
- [ ] Виртуализация списка чатов (react-window) для больших объемов
- [ ] Прогресс-бары для bulk operations
- [ ] Сохранение черновиков ответов
- [ ] Интеграция с реальным WB API для отправки сообщений

### Should Have (P1)
- [ ] Поиск по истории сообщений
- [ ] Фильтры по дате
- [ ] Экспорт переписок
- [ ] Шаблоны ответов
- [ ] История AI генераций с вариантами

### Could Have (P2)
- [ ] Mobile адаптация
- [ ] Темная тема
- [ ] Уведомления о новых сообщениях
- [ ] Статистика по чатам
- [ ] Интеграция с другими маркетплейсами

## Технические детали

### Производительность

- React Query кеширует список чатов на 5 минут
- История сообщений кешируется на 2 минуты
- Используется `keepPreviousData` для smooth transitions
- Debounce для поиска (500ms)

### Типы данных

См. `src/types/chats.ts`:
- `Chat` - основная модель чата
- `ChatMessage` - модель сообщения
- `ChatTag` - типы тегов
- `ChatsResponse` - ответ API со списком чатов

### Стилизация

- Tailwind CSS для всех стилей
- shadcn/ui компоненты (Button, Input, Select, Checkbox, etc.)
- Responsive design (адаптация под разные экраны)
- Анимации и transitions для smooth UX

## Troubleshooting

### Чаты не загружаются

1. Проверьте подключение к PostgreSQL
2. Убедитесь что в базе есть данные чатов
3. Проверьте API ключ в `.env.local`

### AI генерация не работает

1. Проверьте настройки Deepseek API
2. Убедитесь что в `user_settings` заполнен `prompt_chat_reply`
3. Проверьте логи в консоли браузера и сервера

### Массовые операции зависают

1. Уменьшите количество выбранных чатов
2. Проверьте rate limiting на WB API
3. Проверьте логи сервера на ошибки

## Поддержка

Если возникли вопросы или проблемы:
1. Проверьте консоль браузера (F12) на ошибки
2. Проверьте логи Next.js сервера
3. Создайте issue на GitHub с описанием проблемы
