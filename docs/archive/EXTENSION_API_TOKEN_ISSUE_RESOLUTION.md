# Extension API Token Issue - Resolution Summary

**Дата:** 2026-01-29
**Статус:** ✅ RESOLVED
**Приоритет:** 🔥 CRITICAL
**Категория:** Integration Support

---

## 📋 Issue Summary

Команда Chrome Extension (R5 Complaints System) обратилась с критической проблемой:
- Все токены из документации возвращали `401 Unauthorized`
- Блокировалась разработка Multi-Store функционала
- Требовался валидный Bearer Token для production endpoints

---

## 🔍 Root Cause Analysis

### Проблема

Токен из документации: `d794d4408ef0955a693afaa913dd195ddd25f447e94d098c2f6e0155aa2b0038`

**Почему не работал:**

1. Этот токен был сгенерирован для таблицы `api_tokens` (новая store-specific система)
2. НО все Extension API endpoints используют `getUserByApiToken()` из `user_settings`
3. В результате: токен есть в БД, но не в той таблице → 401 error

### Архитектурная причина

В системе параллельно существуют **ДВЕ системы аутентификации**:

| Система | Таблица | Используется в | Статус |
|---------|---------|----------------|--------|
| **User API Keys** | `user_settings.api_key` | `/api/extension/*` | ✅ Production |
| **Store API Tokens** | `api_tokens.token` | Будущие endpoints | 🚧 Not used yet |

**Решение:**
- Extension endpoints используют User API Keys из `user_settings`
- Таблица `api_tokens` была создана преждевременно для будущего функционала

---

## ✅ Решение

### 1. Предоставлен актуальный токен

```
wbrm_0ab7137430d4fb62948db3a7d9b4b997
```

**Характеристики:**
- Формат: `wbrm_*` (32-char hex)
- Источник: `user_settings.api_key`
- Owner: `itsklimovworkspace@gmail.com`
- Доступ: Ко всем 50 магазинам пользователя
- Rate Limit: 100 req/min

---

### 2. Протестированы все endpoints

✅ **GET `/api/extension/stores`**
- Возвращает список всех магазинов пользователя
- Результат: 50 stores (включая `ss6Y8orHTX6vS7SgJl4k`)

✅ **GET `/api/extension/stores/:storeId/complaints`**
- Возвращает жалобы для конкретного магазина
- Query params: `filter`, `limit`, `rating`

✅ **POST `/api/extension/stores/:storeId/reviews/:reviewId/complaint/sent`**
- Отмечает жалобу как отправленную
- Обновляет `complaint_status = 'sent'`

---

### 3. Подтверждена доступность Store ID

**Store:** `ss6Y8orHTX6vS7SgJl4k`
- ✅ Существует в базе
- Name: `20Grace ИП Ширазданова Г. М.`
- Status: `stopped` (inactive, но доступен для чтения)
- Owner: `itsklimovworkspace@gmail.com`

---

## 📦 Deliverables

### 1. Документация для Extension Team

**Файл:** `BACKEND_TOKEN_RESPONSE.md` (в папке Chrome Extension проекта)

**Содержимое:**
- ✅ Актуальный Bearer Token
- ✅ Причина недействительности старых токенов
- ✅ Примеры использования всех endpoints (curl + JavaScript)
- ✅ Security best practices (Chrome Storage API)
- ✅ Rate limiting рекомендации
- ✅ Error handling примеры
- ✅ Integration checklist

---

### 2. Тестовые примеры

**cURL тесты:**

```bash
# 1. Получить список магазинов
curl -H "Authorization: Bearer wbrm_0ab7137430d4fb62948db3a7d9b4b997" \
     http://158.160.217.236/api/extension/stores

# 2. Получить жалобы для магазина
curl -H "Authorization: Bearer wbrm_0ab7137430d4fb62948db3a7d9b4b997" \
     "http://158.160.217.236/api/extension/stores/ss6Y8orHTX6vS7SgJl4k/complaints?limit=10"

# 3. Отметить жалобу как отправленную
curl -X POST \
     -H "Authorization: Bearer wbrm_0ab7137430d4fb62948db3a7d9b4b997" \
     http://158.160.217.236/api/extension/stores/ss6Y8orHTX6vS7SgJl4k/reviews/review_123/complaint/sent
```

**Результат:** Все тесты возвращают 200 OK ✅

---

## 🔄 Action Items

### Для Backend Team (нас)

- [x] Проанализировать проблему с токенами
- [x] Протестировать все Extension API endpoints
- [x] Сгенерировать новый токен (для `api_tokens` - не использован)
- [x] Подтвердить актуальный токен из `user_settings`
- [x] Создать подробную документацию для Extension Team
- [x] Предоставить примеры использования

### Для Extension Team

- [ ] Обновить токен в коде расширения
- [ ] Использовать Chrome Storage API вместо hardcode
- [ ] Протестировать Multi-Store integration end-to-end
- [ ] Обновить внутреннюю документацию
- [ ] Завершить разработку функционала

---

## 📚 Lessons Learned

### 1. Документация

**Проблема:** Документация содержала токен из таблицы `api_tokens`, которая не используется Extension API

**Решение:**
- Обновить все файлы документации с актуальным токеном
- Добавить явное указание формата токена (`wbrm_*`)
- Указать источник токена (`user_settings.api_key`)

### 2. Архитектурная согласованность

**Проблема:** Две параллельные системы аутентификации создали confusion

**Рекомендации:**
- Либо унифицировать на одну систему
- Либо четко разделить use cases:
  - `user_settings.api_key` → Extension API (user-level access)
  - `api_tokens.token` → Store-specific API (single store access)
- Обновить код endpoints для поддержки обеих систем

### 3. Testing процесс

**Проблема:** Токены не были протестированы после migration на v2.0.0

**Рекомендация:**
- Добавить integration tests для всех Extension API endpoints
- Проверять токены из документации перед публикацией
- Создать automated test suite для API authentication

---

## 🎯 Next Steps (Optional)

### Рекомендации для улучшения API

1. **Unified Authentication:**
   ```typescript
   // Поддержка обеих систем токенов
   async function verifyToken(token: string) {
     // 1. Try user_settings (wbrm_* format)
     const userToken = await getUserByApiToken(token);
     if (userToken) return { type: 'user', data: userToken };

     // 2. Try api_tokens (64-char hex)
     const storeToken = await verifyApiToken(`Bearer ${token}`);
     if (storeToken) return { type: 'store', data: storeToken };

     return null;
   }
   ```

2. **Token Management UI:**
   - Админ-панель для генерации/отзыва токенов
   - Просмотр активных токенов и `last_used_at`
   - Автоматическое логирование использования

3. **Rate Limiting Headers:**
   - Уже реализовано для `/api/extension/stores` ✅
   - Добавить в остальные endpoints:
     ```
     X-RateLimit-Limit: 100
     X-RateLimit-Remaining: 95
     X-RateLimit-Reset: 2026-01-29T10:30:00Z
     ```

---

## 📞 Communication

### Outgoing

**Кому:** Extension Development Team (R5 Complaints System)
**Формат:** `BACKEND_TOKEN_RESPONSE.md`
**Статус:** ✅ Sent

**Содержание:**
- Актуальный токен с full documentation
- Примеры интеграции
- Security recommendations
- Testing instructions

### Incoming

**От кого:** Extension Development Team
**Дата:** 2026-01-29
**Формат:** `BACKEND_TOKEN_ISSUE.md`

**Запрос:**
1. Актуальный Bearer Token для production
2. Информация о формате/сроке действия токена
3. Причина недействительности старых токенов

**Статус:** ✅ Все пункты выполнены

---

## 📊 Metrics

**Response Time:** ~1 час (от получения запроса до ответа)

**Endpoints Tested:** 3/3 ✅
- `/api/extension/stores`
- `/api/extension/stores/:storeId/complaints`
- `/api/extension/stores/:storeId/reviews/:reviewId/complaint/sent`

**Stores Available:** 50 total
- Active: 33
- Inactive: 17 (stopped/paused/archived)

**Token Format:** `wbrm_*` (32-char hex)
**Token Source:** `user_settings.api_key`
**Rate Limit:** 100 req/min

---

## ✅ Resolution Summary

**Проблема:** Extension Team не мог использовать Backend API из-за недействительных токенов

**Root Cause:** Токены из документации были для неиспользуемой системы аутентификации (`api_tokens` вместо `user_settings`)

**Решение:**
1. Предоставлен актуальный токен из `user_settings`
2. Протестированы все Extension API endpoints
3. Создана полная документация с примерами
4. Подтверждена доступность всех нужных ресурсов

**Impact:**
- ✅ Extension Team может продолжить разработку
- ✅ Multi-Store интеграция больше не заблокирована
- ✅ Ожидаемое время завершения: 1-2 часа

**Status:** 🟢 **RESOLVED**

---

**Дата создания:** 2026-01-29
**Автор:** Backend Team (WB Reputation Manager)
**Версия API:** 2.0.0
